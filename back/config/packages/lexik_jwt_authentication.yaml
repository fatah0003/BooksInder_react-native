lexik_jwt_authentication:
    secret_key: '%kernel.project_dir%/config/jwt/private.pem'   # Clé privée
    public_key: '%kernel.project_dir%/config/jwt/public.pem'    # Clé publique
    pass_phrase: '%env(JWT_PASSPHRASE)%'                        # Passphrase ou vide
    #token_ttl: 3600                                             # Durée de vie du token
    token_ttl: 14400 #pour tester plus facilement, à enlever et remttre 3600
